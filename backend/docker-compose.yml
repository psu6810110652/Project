services:
  app:
    build: .
    container_name: teerayut_app
    restart: always
    ports:
      - '3000:3000'
    env_file:
      - .env
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules